# Frontend

## Setup

1. Copy `.env.example` to `.env`
2. Install deps with `make frontend-install`
3. Run `make frontend-dev`

If you also want the local backend and Postgres, run `make dev` from the repository root.

## Verification

- `make frontend-test`
- `make frontend-typecheck`
- `make frontend-build`
